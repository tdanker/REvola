# calculate the summary values ("indicators") in the  head of  the spreadsheet tools
#sim<-tool2_table(demand$`2014`, availability$`2014` )
#Net_demand                <- sum(sim$demand/1000) # TWh
#renewables_in_net_demand  <- ((sum(sim$renewable_generation) + sum(sim$storage_discharging))/1000/Net_demand)*100 #%

